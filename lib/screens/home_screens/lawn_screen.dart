import 'package:flutter/material.dart';

class LawnScreen extends StatelessWidget {
  const LawnScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('LawnScreen'),
      ),
    );
  }
}
